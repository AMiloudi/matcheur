
<ul>
  <% @matches.each do |match| %>
<li> <%=match.studenta.name%> is matched with <%= match.studentb.name %> on day: <%=match.day%></li>
<% end %>
</ul>
<%= link_to "show todays match", root_path, class:"button" %>
